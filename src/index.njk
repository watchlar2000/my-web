---
layout: layouts/base.njk
title: 'Home'
---

<div class="cards-grid">
  <article class="wrapper | card card-about" data-wrapper-type="inner">
    <h2 class="text-break | card-title">about</h2>
    <p class="card-content">
      Hello! My name's Sergii and I craft web applications that are fast,
      accessible, good looking and delightful to use.
    </p>
    <a href="/about" class="card-link">
      <span class="visually-hidden">Go to about page to read more</span>
      {% svg "src/images/icons/arrow-right.svg" %}
    </a>
  </article>
  <article class="wrapper | card card-now" data-wrapper-type="inner">
    <h2 class="card-title">now</h2>
    <p class="card-content">See what is happening in my life.</p>
    <a href="/now" class="card-link">
      <span class="visually-hidden">Go to now page to read more</span>
      {% svg "src/images/icons/arrow-right.svg" %}
    </a>
  </article>
  <article class="wrapper | card card-projects" data-wrapper-type="inner">
    <h2 class="text-break | card-title">projects</h2>
    <ul role="list" class="flow | projects-list">
      {% set featuredProjects = collections.project %} {% for project in
      featuredProjects %} {% if project.data.featured %}
      <li class="flow-tight | card-content project-item">
        <a href="{{ project.url }}" class="text-break | project-item__title"
          >{{ project.data.title }}</a
        >
        <p>{{ project.data.description }}</p>
        <div>
          <ul role="list" class="cluster | project-item__tags">
            {% for tag in project.data.tech %} {# {% if forloop.index <= 4 %} #}
            <li>{{ tag }}</li>
            {# {% endif %} #} {% endfor %}
          </ul>
        </div>
      </li>
      {% endif %} {% endfor %}
    </ul>
  </article>
  <div class="flow-tight | card card-decoration">
    <div>
      {% image {alt: "Cubes and a duck", source:
      "src/images/theme/cubes-white.png"} %}
    </div>
    <a href="mailto:sergii.koch@gmail.com">Contact me</a>
  </div>
</div>
